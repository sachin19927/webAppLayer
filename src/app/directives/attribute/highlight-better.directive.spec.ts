import { HighlightBetterDirective } from './highlight-better.directive';

describe('HighlightBetterDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightBetterDirective();
    expect(directive).toBeTruthy();
  });
});

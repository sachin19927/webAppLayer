import { ConvertDateFormat } from './convert-date-format';

describe('ConvertDateFormat', () => {
  it('should create an instance', () => {
    expect(new ConvertDateFormat()).toBeTruthy();
  });
});

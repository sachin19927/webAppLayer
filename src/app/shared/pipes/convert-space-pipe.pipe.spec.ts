import { ConvertSpacePipePipe } from './convert-space-pipe.pipe';

describe('ConvertSpacePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertSpacePipePipe();
    expect(pipe).toBeTruthy();
  });
});

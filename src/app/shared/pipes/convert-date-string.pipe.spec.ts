import { ConvertDateStringPipe } from './convert-date-string.pipe';

describe('ConvertDateStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDateStringPipe();
    expect(pipe).toBeTruthy();
  });
});
